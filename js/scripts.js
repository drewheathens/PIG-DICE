// $(document).ready(function(){
//   $("button").onclick(rollDice(event){
function(rollDice());
    var d1 = math.floor(math.random() *6) +1;


    var d1 = document.getElementById(die);
    var status = document.getElementById('status');

    var dieTotal.innerHTML = d1 + d1;
    innerHTML = "rolled" + dieTotal + ".";

    if (d1 == 1) {
      status.innerHTML += hold..
    }
    else if(d1 == d1) {
      status.innerHTML += "free turn"
    }
    d1.innerHTML = d1;
  });
});
